# CodeImagine 🎨 (v1.1.0)

**CodeImagine** هي أداة CLI مبتكرة مبنية باستخدام Node.js تقوم بتوليد صور فنية بناءً على معادلات رياضية وأبعاد مستوحاة من الأوصاف النصية، مع ميزة فريدة وهي ضغط الصورة بالكامل داخل رمز QR وعرضه مباشرة في التيرمينال.

## المميزات الجديدة ✨
- **محرك رياضي معقد**: يدعم الآن الأنماط الهندسية المتداخلة، الألوان المتعددة، والشبكات الفنية.
- **QR في التيرمينال**: يظهر رمز QR فوراً بعد التوليد داخل واجهة سطر الأوامر.
- **ضغط البيانات (Data URI)**: يتم ضغط الصورة وتحويلها إلى Base64 وتضمينها داخل الـ QR، مما يعني أن الصورة "تعيش" داخل الرمز نفسه ولا تحتاج لرابط خارجي.
- **أمان كامل**: العملية محلية 100%، لا يتم رفع صورك لأي خادم.

## التثبيت 🚀
```bash
git clone https://github.com/global-developers-labs/code-imagine.git
cd code-imagine
npm install
npm link
```

## الاستخدام 🛠️
```bash
code-imagine generate "أنماط هندسية معقدة بألوان حمراء وزرقاء"
```

## الأسئلة الشائعة ❓
### هل مسح رمز QR آمن؟
نعم، آمن تماماً. الرمز لا يحتوي على رابط لموقع خارجي قد يكون ضاراً، بل يحتوي على "بيانات الصورة" نفسها بتنسيق Base64. عند مسحه، سيقوم هاتفك بفتح الصورة مباشرة باستخدام مستعرض الصور أو المتصفح المحلي.

### كيف يتم ضغط الصورة؟
نستخدم مكتبة `sharp` لتقليل أبعاد الصورة وجودتها لدرجة تسمح بتخزينها داخل حدود سعة الـ QR Code، مما يضمن سرعة الفتح والاستقلالية التامة.

## الترخيص 📄
MIT
